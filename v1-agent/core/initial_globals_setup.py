# Initial Agent Global State (Schematic)

_AGENT_TOOLS = {}
_TOOL_DESCRIPTIONS = {}
_AGENT_VERSION = "0.1.0"

# Note: The actual add_tool function's source can be found in core_helper_functions.py (if exported)
# or as one of the tool .py files if it was overwritten and became a tool itself.
